import React from "react";

const context = React.createContext({
  // ingredientAdded: () => {},
  // ingredientRemove: () => {},
  // disabledInfo: {},
  // purchasable: false,
});

export default context;
